
// Stub configuration for overlay to prevent blank screen if missing.
window.CONFIG = window.CONFIG || {};
window.OVERLAY_CONFIG = window.OVERLAY_CONFIG || {
    theme: "default",
    animationSpeed: 1.0,
};
console.log("[config.js] Loaded default overlay configuration.");
